# Система управления аптечным складом

Программное обеспечение для автоматизации учета товаров, контроля движения (приход/расход) и формирования отчетности на аптечном складе.

## 📋 Ключевые функции
*   **Управление номенклатурой:** Ведение базы лекарственных средств и товаров аптечного ассортимента.
*   **Складские операции:** Оформление приходных и расходных накладных, проведение инвентаризации.
*   **Контроль сроков:** Автоматическое отслеживание и оповещение об истекающих сроках годности.
*   **Отчетность:** Формирование отчетов по остаткам, оборачиваемости и движению товаров.

## 🛠 Технологический стек
*   **Язык программирования:** C#
*   **Фреймворк и интерфейс:** .NET, Windows Forms
*   **Система управления базами данных:** Microsoft SQL Server / SQLite
*   **Архитектурный паттерн:** MVVM / Многослойная архитектура

## 🚀 Установка и запуск
1.  **Клонировать репозиторий:**
    ```bash
    git clone https://your-repository-link.git
    ```
2.  **Восстановить зависимости:** Открыть решение (`PharmacyWarehouse.sln`) в Visual Studio и выполнить восстановление пакетов NuGet.
3.  **Настроить базу данных:**
    *   Выполнить скрипт инициализации БД (`Database/Schema.sql`).
    *   Обновить строку подключения в файле `App.config`.
4.  **Запустить проект:** Установить `PharmacyWarehouse` как запускаемый проект и нажать **F5**.

# Система управления аптечным складом

Программа для автоматизации учета лекарств на аптечном складе: приход, расход, контроль сроков годности и отчетность.

## 📋 Возможности
*   Добавление и редактирование лекарств в справочнике
*   Оформление приходных и расходных накладных
*   Контроль сроков годности с оповещением
*   Формирование отчетов по остаткам

## 🛠 Технологии
*   C#, .NET, Windows Forms
*   MS SQL Server / SQLite
*   Entity Framework (миграции)
*   Многослойная архитектура

## 🚀 Быстрый старт

### 1. Установка
    ```bash
    git clone https://github.com/ваш-логин/pharmacy-warehouse.git
    ```
###2. Настройка базы данных
Способ 1: Через консоль диспетчера пакетов (Visual Studio)

Откройте PharmacyWarehouse.sln в Visual Studio

Перейдите: Инструменты → Диспетчер пакетов NuGet → Консоль диспетчера пакетов

Выполните команду:

```powershell
Update-Database
```
Способ 2: Через командную строку (.NET CLI)

```bash
dotnet ef database update
```
###3. Настройка подключения к БД
Откройте файл App.config в проекте и проверьте строку подключения:

Для SQL Server:

```xml
<connectionStrings>
  <add name="DefaultConnection" 
       connectionString="Server=(localdb)\mssqllocaldb;Database=PharmacyWarehouseDB;Trusted_Connection=True;" />
</connectionStrings>
```
###4. Запуск
В Visual Studio установите проект PharmacyWarehouse как запускаемый

Нажмите F5 или Запуск



```powershell
# В консоли диспетчера пакетов:
Add-Migration "ОписаниеИзменений"
Update-Database
```
