<Page x:Class="PharmacyWarehouse.Pages.MovementReportPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:PharmacyWarehouse.Pages"
      mc:Ignorable="d" 
      Title="Отчёт по движению товаров">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>

            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,20">
            <TextBlock Text="📊" FontSize="28" VerticalAlignment="Center" Margin="0,0,12,0"/>
            <StackPanel>
                <TextBlock Text="Отчёт по движению товаров" Style="{StaticResource H1}"/>
                <TextBlock Text="Анализ прихода, расхода и остатков товаров за выбранный период" 
                           Style="{StaticResource Caption}"/>
            </StackPanel>
        </StackPanel>

        <!-- Параметры отчёта -->
        <Border Grid.Row="1" Style="{StaticResource Card}">
            <StackPanel>
                <TextBlock Text="Параметры отчёта" Style="{StaticResource H3}" Margin="0,0,0,12"/>

                <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                    <StackPanel Margin="0,0,20,0">
                        <TextBlock Text="Период:" Style="{StaticResource Caption}"/>
                        <StackPanel Orientation="Horizontal">
                            <DatePicker x:Name="dpReportFrom" Width="120" Margin="0,0,8,0"/>
                            <TextBlock Text="—" VerticalAlignment="Center" Margin="0,0,8,0"/>
                            <DatePicker x:Name="dpReportTo" Width="120"/>
                        </StackPanel>
                    </StackPanel>

                    <StackPanel Margin="0,0,20,0">
                        <TextBlock Text="Товар:" Style="{StaticResource Caption}"/>
                        <ComboBox x:Name="cbProduct" Width="200">
                            <ComboBoxItem Content="Все товары"/>
                        </ComboBox>
                    </StackPanel>

                    <StackPanel Margin="0,0,20,0">
                        <TextBlock Text="Категория:" Style="{StaticResource Caption}"/>
                        <ComboBox x:Name="cbCategory" Width="180">
                            <ComboBoxItem Content="Все категории"/>
                        </ComboBox>
                    </StackPanel>

                    <StackPanel>
                        <TextBlock Text="Группировка:" Style="{StaticResource Caption}"/>
                        <ComboBox x:Name="cbGroupBy" Width="150" SelectedIndex="0">
                            <ComboBoxItem Content="По дням"/>
                            <ComboBoxItem Content="По неделям"/>
                            <ComboBoxItem Content="По месяцам"/>
                            <ComboBoxItem Content="По товарам"/>
                            <ComboBoxItem Content="По категориям"/>
                        </ComboBox>
                    </StackPanel>
                </StackPanel>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="📈 Сформировать отчёт" Style="{StaticResource PrimaryButton}" 
                            Margin="0,0,8,0"/>
                    <Button Content="🔄 Сбросить" Style="{StaticResource OutlineButton}"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- Сводная информация -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,12,0,12">
            <Border Style="{StaticResource Card}" Margin="0,0,12,0" Width="180">
                <StackPanel>
                    <TextBlock Text="Начальный остаток" Style="{StaticResource Caption}"/>
                    <TextBlock x:Name="txtStartBalance" Text="0" FontSize="20" 
                               FontWeight="Bold" Foreground="{StaticResource PrimaryBrush}"/>
                    <TextBlock Text="шт." Style="{StaticResource Caption}"/>
                </StackPanel>
            </Border>

            <Border Style="{StaticResource Card}" Margin="0,0,12,0" Width="180">
                <StackPanel>
                    <TextBlock Text="Приход за период" Style="{StaticResource Caption}"/>
                    <TextBlock x:Name="txtReceiptTotal" Text="0" FontSize="20" 
                               FontWeight="Bold" Foreground="{StaticResource AccentGreenBrush}"/>
                    <TextBlock Text="шт." Style="{StaticResource Caption}"/>
                </StackPanel>
            </Border>

            <Border Style="{StaticResource Card}" Margin="0,0,12,0" Width="180">
                <StackPanel>
                    <TextBlock Text="Расход за период" Style="{StaticResource Caption}"/>
                    <TextBlock x:Name="txtExpenseTotal" Text="0" FontSize="20" 
                               FontWeight="Bold" Foreground="{StaticResource AccentOrangeBrush}"/>
                    <TextBlock Text="шт." Style="{StaticResource Caption}"/>
                </StackPanel>
            </Border>

            <Border Style="{StaticResource Card}" Width="180">
                <StackPanel>
                    <TextBlock Text="Конечный остаток" Style="{StaticResource Caption}"/>
                    <TextBlock x:Name="txtEndBalance" Text="0" FontSize="20" 
                               FontWeight="Bold" Foreground="{StaticResource SecondaryBrush}"/>
                    <TextBlock Text="шт." Style="{StaticResource Caption}"/>
                </StackPanel>
            </Border>
        </StackPanel>

        <!-- Данные отчёта -->
        <DataGrid x:Name="dgReport" Grid.Row="3" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Период/Товар" Width="200"/>
                <DataGridTextColumn Header="Начальный остаток" Binding="{Binding StartBalance}" Width="120" 
                                    CellStyle="{StaticResource DataGridCellRight}"/>
                <DataGridTextColumn Header="Приход" Binding="{Binding Receipt}" Width="100" 
                                    CellStyle="{StaticResource DataGridCellRight}">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="Foreground" Value="{StaticResource AccentGreenBrush}"/>
                            <Setter Property="FontWeight" Value="SemiBold"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="Расход" Binding="{Binding Expense}" Width="100" 
                                    CellStyle="{StaticResource DataGridCellRight}">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="Foreground" Value="{StaticResource AccentOrangeBrush}"/>
                            <Setter Property="FontWeight" Value="SemiBold"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="Конечный остаток" Binding="{Binding EndBalance}" Width="120" 
                                    CellStyle="{StaticResource DataGridCellRight}"/>
                <DataGridTextColumn Header="Изменение, %" Binding="{Binding ChangePercent, StringFormat=\{0:N1\} %}" 
                                    Width="100" CellStyle="{StaticResource DataGridCellRight}">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding ChangePercent}" Value="0">
                                    <Setter Property="Foreground" Value="{StaticResource GrayBrush}"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding ChangePercent}">
                                    <Setter Property="Foreground" Value="{StaticResource AccentGreenBrush}"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding ChangePercent}">
                                    <Setter Property="Foreground" Value="{StaticResource AccentRedBrush}"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="Средний запас" Binding="{Binding AvgStock}" Width="100" 
                                    CellStyle="{StaticResource DataGridCellRight}"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Панель экспорта -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="📊 Диаграмма" Style="{StaticResource SecondaryButton}" 
                    Margin="0,0,8,0"/>
            <Button Content="📄 Печать отчёта" Style="{StaticResource PrimaryButton}" 
                    Margin="0,0,8,0"/>
            <Button Content="💾 Экспорт в Excel" Style="{StaticResource SuccessButton}" 
                    Margin="0,0,8,0"/>
            <Button Content="📧 Отправить" Style="{StaticResource OutlineButton}"/>
        </StackPanel>
    </Grid>
</Page>
