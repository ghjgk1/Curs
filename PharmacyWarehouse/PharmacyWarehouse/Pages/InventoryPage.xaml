<Page x:Class="PharmacyWarehouse.Pages.InventoryPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:PharmacyWarehouse.Pages"
      mc:Ignorable="d" 
      Title="Инвентаризация">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- СОЗДАНИЕ НОВОЙ ИНВЕНТАРИЗАЦИИ -->
        <Border Style="{StaticResource Card}">
            <StackPanel>
                <TextBlock Text="Инвентаризация товаров" Style="{StaticResource H1}"/>

                <Grid Margin="0,16,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- Форма создания -->
                    <StackPanel Grid.Column="0">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="150"/>
                                <ColumnDefinition Width="200"/>
                                <ColumnDefinition Width="150"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Text="Дата инвентаризации:" Grid.Column="0" 
                                      Style="{StaticResource BodyText}"
                                      VerticalAlignment="Center"/>
                            <DatePicker x:Name="dpInventoryDate" 
                                       Grid.Column="1"
                                       Margin="8,0,0,0"/>

                            <TextBlock Text="Ответственный:" Grid.Column="2" 
                                      Style="{StaticResource BodyText}"
                                      VerticalAlignment="Center"
                                      Margin="16,0,0,0"/>
                            <TextBox x:Name="txtResponsible" 
                                    Grid.Column="3"
                                    Margin="8,0,0,0"/>
                        </Grid>

                        <WrapPanel Margin="0,16,0,0">
                            <CheckBox Content="Только товары с остатком" Margin="0,0,16,0"/>
                            <CheckBox Content="Включая архивные" Margin="0,0,16,0"/>
                            <CheckBox Content="Только товары с истекающим сроком"/>
                        </WrapPanel>
                    </StackPanel>

                    <!-- Кнопка создания -->
                    <Button Grid.Column="1" 
                           Content="Начать инвентаризацию" 
                           Style="{StaticResource SuccessButton}"
                           Width="200"
                           Height="40"
                           VerticalAlignment="Bottom"
                           Margin="24,0,0,0"/>
                </Grid>
            </StackPanel>
        </Border>

        <!-- СПИСОК ИНВЕНТАРИЗАЦИЙ -->
        <Border Style="{StaticResource Card}" Grid.Row="1" Margin="0,16,0,16">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Заголовок -->
                <Border Grid.Row="0" Style="{StaticResource CardHeader}">
                    <TextBlock Text="История инвентаризаций" 
                              Style="{StaticResource H3}" 
                              Foreground="{StaticResource TextWhiteBrush}"/>
                </Border>

                <!-- Таблица -->
                <DataGrid x:Name="dgInventories" 
                         Grid.Row="1"
                         AutoGenerateColumns="False">

                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Номер" Width="80" Binding="{Binding InventoryId}"/>
                        <DataGridTextColumn Header="Дата" Width="100" 
                                           Binding="{Binding InventoryDate, StringFormat='dd.MM.yyyy'}"/>
                        <DataGridTextColumn Header="Ответственный" Width="150" 
                                           Binding="{Binding ResponsiblePerson}"/>

                        <DataGridTemplateColumn Header="Статус" Width="120">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Border CornerRadius="12" 
                                           Padding="6,2"
                                           HorizontalAlignment="Center">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Setter Property="Background" Value="{StaticResource GrayLightBrush}"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsCompleted}" Value="True">
                                                        <Setter Property="Background" Value="{StaticResource AccentGreenBrush}"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding IsCompleted}" Value="False">
                                                        <Setter Property="Background" Value="{StaticResource AccentOrangeBrush}"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <TextBlock Text="{Binding StatusText}"
                                                  Foreground="White"
                                                  FontWeight="SemiBold"
                                                  HorizontalAlignment="Center"/>
                                    </Border>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <DataGridTextColumn Header="Позиций" Width="80" Binding="{Binding ItemsCount}">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="HorizontalAlignment" Value="Center"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <DataGridTextColumn Header="Расхождения" Width="100" Binding="{Binding DiscrepancyCount}">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="HorizontalAlignment" Value="Center"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding HasDiscrepancies}" Value="True">
                                            <Setter Property="Foreground" Value="{StaticResource AccentRedBrush}"/>
                                            <Setter Property="FontWeight" Value="Bold"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <DataGridTemplateColumn Header="Действия" Width="200">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                        <Button Content="Продолжить" 
                                               Style="{StaticResource PrimaryButton}"
                                               Padding="8,4"
                                               FontSize="12"
                                               Visibility="{Binding IsCompleted, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter=inverse}"/>
                                        <Button Content="Результаты" 
                                               Style="{StaticResource OutlineButton}"
                                               Padding="8,4"
                                               FontSize="12"
                                               Margin="8,0,0,0"/>
                                        <Button Content="Печать" 
                                               Style="{StaticResource SecondaryButton}"
                                               Padding="8,4"
                                               FontSize="12"
                                               Margin="8,0,0,0"/>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>

        <!-- СТАТИСТИКА -->
        <Border Style="{StaticResource Card}" Grid.Row="2">
            <StackPanel>
                <TextBlock Text="Статистика инвентаризаций" Style="{StaticResource H3}" Margin="0,0,0,12"/>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Всего инвентаризаций -->
                    <Border Grid.Column="0" 
                           Background="{StaticResource BackgroundLightBrush}"
                           Padding="16"
                           BorderBrush="{StaticResource BorderBrush}"
                           BorderThickness="1"
                           CornerRadius="4">
                        <StackPanel>
                            <TextBlock Text="Всего инвентаризаций" Style="{StaticResource Caption}"/>
                            <TextBlock x:Name="txtTotalInventories" Text="0" 
                                      FontSize="24"
                                      FontWeight="Bold"
                                      Foreground="{StaticResource PrimaryBrush}"/>
                        </StackPanel>
                    </Border>

                    <!-- Завершено -->
                    <Border Grid.Column="1" 
                           Margin="12,0,0,0"
                           Background="{StaticResource BackgroundLightBrush}"
                           Padding="16"
                           BorderBrush="{StaticResource BorderBrush}"
                           BorderThickness="1"
                           CornerRadius="4">
                        <StackPanel>
                            <TextBlock Text="Завершено" Style="{StaticResource Caption}"/>
                            <TextBlock x:Name="txtCompletedInventories" Text="0" 
                                      FontSize="24"
                                      FontWeight="Bold"
                                      Foreground="{StaticResource AccentGreenBrush}"/>
                        </StackPanel>
                    </Border>

                    <!-- В процессе -->
                    <Border Grid.Column="2" 
                           Margin="12,0,0,0"
                           Background="{StaticResource BackgroundLightBrush}"
                           Padding="16"
                           BorderBrush="{StaticResource BorderBrush}"
                           BorderThickness="1"
                           CornerRadius="4">
                        <StackPanel>
                            <TextBlock Text="В процессе" Style="{StaticResource Caption}"/>
                            <TextBlock x:Name="txtInProgressInventories" Text="0" 
                                      FontSize="24"
                                      FontWeight="Bold"
                                      Foreground="{StaticResource AccentOrangeBrush}"/>
                        </StackPanel>
                    </Border>

                    <!-- Расхождения -->
                    <Border Grid.Column="3" 
                           Margin="12,0,0,0"
                           Background="{StaticResource BackgroundLightBrush}"
                           Padding="16"
                           BorderBrush="{StaticResource BorderBrush}"
                           BorderThickness="1"
                           CornerRadius="4">
                        <StackPanel>
                            <TextBlock Text="Общие расхождения" Style="{StaticResource Caption}"/>
                            <TextBlock x:Name="txtTotalDiscrepancies" Text="0" 
                                      FontSize="24"
                                      FontWeight="Bold"
                                      Foreground="{StaticResource AccentRedBrush}"/>
                        </StackPanel>
                    </Border>
                </Grid>
            </StackPanel>
        </Border>

    </Grid>
</Page>